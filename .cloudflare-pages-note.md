# Cloudflare Pages automatically detects and builds Astro projects
# No build configuration file needed - Cloudflare intelligently detects:
# - Framework: Astro
# - Build command: npm run build
# - Build output directory: dist
# - Node version: Latest LTS

# For custom configuration, you can create a deployment via:
# 1. Push to GitHub
# 2. Connect repository to Cloudflare Pages
# 3. Cloudflare auto-detects settings
# 4. Deploy!

# Environment variables (if needed):
# None required for static Astro builds
