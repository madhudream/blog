---
import EssayPost from '../../layouts/EssayPost.astro';
import { marked } from 'marked';
import markdownContent from '../../../content/paged-attention.md?raw';

// Strip YAML frontmatter + the h1 title line — both rendered by the layout header
const lines = markdownContent.split('\n');
const contentStart = lines.findIndex(l => l.startsWith('# '));
const markdownToRender = lines.slice(contentStart + 1).join('\n');

const content = await marked.parse(markdownToRender);
---

<EssayPost
  title="PagedAttention: How vLLM Stops Wasting KV Cache Memory"
  date="July 25, 2026"
  readTime="4 min read"
  category="LLM Inference · Technique"
  description="Store the KV cache in small fixed-size blocks handed out on demand instead of one big reserved chunk per request. The memory idea at the heart of vLLM."
>
  <Fragment set:html={content} />
</EssayPost>
