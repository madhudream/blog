---
import EssayPost from '../../layouts/EssayPost.astro';
import { marked } from 'marked';
import markdownContent from '../../../content/llm-inference-metrics.md?raw';

// Strip YAML frontmatter + the h1 title line — both rendered by the layout header
const lines = markdownContent.split('\n');
const contentStart = lines.findIndex(l => l.startsWith('# '));
const markdownToRender = lines.slice(contentStart + 1).join('\n');

const content = await marked.parse(markdownToRender);
---

<EssayPost
  title="LLM Inference Metrics: TTFT, TPOT, Throughput, and Latency"
  date="July 25, 2026"
  readTime="5 min read"
  category="LLM Inference · Foundations"
  description="The four numbers that define serving performance, which phase of inference owns each one, and the core trade-off between latency and throughput."
>
  <Fragment set:html={content} />
</EssayPost>
